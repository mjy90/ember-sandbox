<div class='resource'>
  <div class='image'>
    <img src={{this.imageUrl @resource}} alt='Preview image' />
  </div>
  <div class='details'>
    <h3>{{@resource.title}}</h3>
    <p class='metadata'>{{@location.name}} • {{this.exchangeLabel @resource}}</p>
    <p class='description'>{{@resource.description}}</p>
    <button type='button' {{ on 'click' (fn this.signUp @resource) }}>
      Sign Up
    </button>
  </div>
</div>
